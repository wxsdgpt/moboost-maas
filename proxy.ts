// DEPRECATED — Clerk middleware now lives at `src/middleware.ts` per
// the Next.js middleware convention.  This file has no effect on the
// running app (Next.js does not load `proxy.ts`).
//
// Safe to delete manually from your editor / file explorer.
export {}
