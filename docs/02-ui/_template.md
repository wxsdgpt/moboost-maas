# UI 设计模板：<模块名>

> 复制本文件改名即可。每个模块一份，与 PRD 对齐。

## 1. 设计目标

（一句话：希望用户看到什么样的界面、达到什么情绪/效率目标）

## 2. 主要页面 & 路由

| 页面 | 路由 | 说明 |
|------|------|------|
| （如：执行页） | `/brief/execute` | … |

## 3. 信息架构（页面结构）

```
┌──────────────────────────────┐
│ 顶部导航                     │
├──────────────────────────────┤
│ 左侧：输入区                 │
│ ──────  ────────             │
│         主内容区             │
└──────────────────────────────┘
```

（附 Figma 链接或手绘 wireframe）

## 4. 关键交互

- 交互 1：用户点击 X → 显示 Y（loading / skeleton）→ 返回 Z
- 交互 2：…

## 5. 状态设计

| 状态 | 触发 | 视觉表现 |
|------|------|---------|
| Empty | 无数据 | 空状态插画 + 引导 CTA |
| Loading | 请求中 | 骨架屏 + ThinkingPanel |
| Error | 失败 | 红色 banner + 重试按钮 |
| Success | 完成 | 卡片列表 |

## 6. 视觉规范

引用 [设计 tokens](./design-tokens.md)，不自定义颜色/字号。

## 7. 组件清单

- 新组件：（放到 `src/components/` 或模块内）
- 复用组件：（从 `src/components/` 引入）

## 8. 响应式

- 断点：见 design-tokens `breakpoints`
- Mobile 行为：…
- Tablet：…
- Desktop：…

## 9. 可访问性（a11y）

- 所有按钮有 `aria-label`
- 键盘可达
- 色彩对比度 ≥ WCAG AA

## 10. 验收走查

- [ ] 所有状态（empty/loading/error/success）都已设计
- [ ] 移动端在 iPhone SE（375px）不溢出
- [ ] 颜色/字号都来自 design tokens
- [ ] 与 PRD 验收标准一一对应
